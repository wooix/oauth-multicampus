<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multi.contactsapp.mapper.ContactMapper">
	<select id="getContactList" resultType="Contact">
		SELECT no, name, tel, address
		FROM contact
		ORDER BY no DESC
	</select>
	<select id="getContactOne" parameterType="Contact"
		resultType="Contact">
		SELECT no, name, tel, address
		FROM contact
		WHERE no=#{no}
	</select>
	<insert id="insertContact" parameterType="Contact"
		useGeneratedKeys="true" keyProperty="no">
		INSERT INTO contact (name, tel,
		address)
		VALUES (#{name}, #{tel}, #{address})
	</insert>
	<select id="selectcnt" resultType="int">
		SELECT count(*) FROM contact
	</select>
	<update id="updateContact" parameterType="Contact">
		UPDATE contact
		SET name=#{name}, tel=#{tel}, address=#{address}
		WHERE no=#{no}
	</update>
	<delete id="deleteContact" parameterType="Contact">
		DELETE contact WHERE no=#{no}
	</delete>
</mapper>